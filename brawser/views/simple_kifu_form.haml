%form{action: "/simple_kifu_form", method: "get", :class => "form-vertical"}
  .control-group
    %label{:class => "control-label"}= "棋譜データ"
    .controls
      %tt
        %textarea{:name => "body", :class => "input-xxlarge", :rows => [10, params[:body].to_s.lines.count].max, :style => "font-family:monospace", :placeholder => "▲７六歩 △３四歩 ▲２六歩 ..."}
          = preserve do
            = params[:body]
  .form-actions
    %input{type: "submit", value: "実行", :class => "btn btn-primary"}

= haml :pattern
